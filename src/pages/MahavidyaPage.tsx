import { useState, useEffect } from 'react';
import { MAHAVIDYAS } from '@/data/newcontent';
import { addToHistory } from '@/lib/storage';
import { Modal } from '@/components/Modal';
import { SafetyBadge } from '@/components/SafetyBadge';
import { Moon, ChevronRight, AlertTriangle, BookOpen, MapPin, Sparkles, Shield, Clock } from 'lucide-react';

export function MahavidyaPage() {
  const [selected, setSelected] = useState<typeof MAHAVIDYAS[0] | null>(null);
  useEffect(() => { addToHistory('Mahavidya Portal'); }, []);
  return (
    <div className="space-y-6 animate-fadeIn">
      <div><h1 className="section-title text-3xl text-accent mb-2">Mahavidya Portal</h1><p className="text-muted text-sm">The Ten Great Wisdom Goddesses of the Shakta tradition. Each is a complete cosmic principle and a path to realization. Advanced practices require Diksha; devotional study is open to all.</p></div>
      <div className="card p-4 border-l-2 border-red-500/50 bg-red-500/5"><div className="flex items-start gap-2"><AlertTriangle className="h-5 w-5 text-red-400 shrink-0 mt-0.5" /><p className="text-sm text-muted leading-relaxed"><span className="text-red-400 font-semibold">Important:</span> Bija mantra sadhana, nyasa, and chakra practices related to the Mahavidyas require formal Diksha from a qualified Guru. The content here is preserved for academic study and devotional understanding. Do not attempt advanced practices without initiation.</p></div></div>
      <div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-4">{MAHAVIDYAS.map((m) => (<button key={m.id} onClick={() => setSelected(m)} className="card p-5 text-left hover:border-[var(--accent)] transition-all hover:-translate-y-0.5 group"><div className="flex items-start justify-between gap-2 mb-3"><div><div className="flex items-center gap-2 mb-1"><span className="h-7 w-7 rounded-full bg-gold/15 flex items-center justify-center text-accent font-bold text-xs">{m.number}</span><h3 className="font-semibold">{m.name}</h3></div><p className="iast text-xs text-accent ml-9">{m.iast}</p></div><SafetyBadge level={m.id === 'kamala' ? 'amber' : 'red'} compact /></div><p className="text-xs text-muted leading-relaxed line-clamp-3">{m.symbolism}</p><div className="flex items-center gap-1 mt-3 text-xs text-accent">Explore <ChevronRight className="h-3 w-3" /></div></button>))}</div>
      <Modal open={!!selected} onClose={() => setSelected(null)} title={selected ? `${selected.name} — ${selected.iast}` : ''}>{selected && (<div className="space-y-5"><div className="flex items-center gap-3"><span className="chip">Mahavidya #{selected.number}</span><span className="chip">{selected.color}</span><SafetyBadge level={selected.id === 'kamala' ? 'amber' : 'red'} compact /></div><div><h4 className="text-xs font-semibold text-accent uppercase tracking-wide mb-1">Origin</h4><p className="text-sm leading-relaxed">{selected.origin}</p></div><div><h4 className="text-xs font-semibold text-accent uppercase tracking-wide mb-1">Symbolism</h4><p className="text-sm leading-relaxed">{selected.symbolism}</p></div><div><h4 className="text-xs font-semibold text-accent uppercase tracking-wide mb-1">Philosophy</h4><p className="text-sm leading-relaxed">{selected.philosophy}</p></div><div><h4 className="text-xs font-semibold text-accent uppercase tracking-wide mb-1">Iconography</h4><p className="text-sm leading-relaxed">{selected.iconography}</p></div><div className="grid sm:grid-cols-2 gap-3"><div><h4 className="text-xs font-semibold text-accent uppercase tracking-wide mb-1">Yantra</h4><p className="text-sm">{selected.yantra}</p></div><div><h4 className="text-xs font-semibold text-accent uppercase tracking-wide mb-1">Weapons</h4><div className="flex flex-wrap gap-1.5">{selected.weapons.map((w, i) => <span key={i} className="chip text-[10px]">{w}</span>)}</div></div></div><div><h4 className="text-xs font-semibold text-accent uppercase tracking-wide mb-2 flex items-center gap-1.5"><BookOpen className="h-3 w-3" /> Associated Scriptures</h4><div className="flex flex-wrap gap-1.5">{selected.scriptures.map((s, i) => <span key={i} className="chip text-[10px]">{s}</span>)}</div></div><div><h4 className="text-xs font-semibold text-accent uppercase tracking-wide mb-2 flex items-center gap-1.5"><MapPin className="h-3 w-3" /> Important Temples</h4><div className="space-y-1">{selected.temples.map((t, i) => <p key={i} className="text-sm text-muted">{t}</p>)}</div></div><div><h4 className="text-xs font-semibold text-accent uppercase tracking-wide mb-2 flex items-center gap-1.5"><Clock className="h-3 w-3" /> Festivals</h4><div className="flex flex-wrap gap-1.5">{selected.festivals.map((f, i) => <span key={i} className="chip text-[10px]">{f}</span>)}</div></div><div><h4 className="text-xs font-semibold text-accent uppercase tracking-wide mb-2 flex items-center gap-1.5"><Shield className="h-3 w-3" /> Safe Devotional Practices</h4><ul className="text-sm space-y-1 list-disc list-inside text-muted">{selected.safePractices.map((p, i) => <li key={i}>{p}</li>)}</ul></div><div><h4 className="text-xs font-semibold text-accent uppercase tracking-wide mb-1">Lineage</h4><p className="text-sm leading-relaxed">{selected.lineage}</p></div><div><h4 className="text-xs font-semibold text-accent uppercase tracking-wide mb-2">Frequently Misunderstood Concepts</h4><div className="space-y-2">{selected.misunderstoodConcepts.map((c, i) => <p key={i} className="text-sm text-muted leading-relaxed">{c}</p>)}</div></div><div className="card-2 p-3 border-l-2 border-red-500/50"><h4 className="text-xs font-semibold text-red-400 uppercase tracking-wide mb-1 flex items-center gap-1.5"><AlertTriangle className="h-3 w-3" /> Traditional Cautions</h4><p className="text-sm text-muted leading-relaxed">{selected.cautions}</p></div>{selected.researchRefs.length > 0 && (<div><h4 className="text-xs font-semibold text-accent uppercase tracking-wide mb-2 flex items-center gap-1.5"><Sparkles className="h-3 w-3" /> Academic References</h4><ul className="text-sm space-y-1 list-disc list-inside text-muted">{selected.researchRefs.map((r, i) => <li key={i}>{r}</li>)}</ul></div>)}{selected.relatedBhairavas && selected.relatedBhairavas.length > 0 && (<div><h4 className="text-xs font-semibold text-accent uppercase tracking-wide mb-1">Related Bhairavas</h4><div className="flex flex-wrap gap-1.5">{selected.relatedBhairavas.map((b, i) => <span key={i} className="chip text-[10px]">{b}</span>)}</div></div>)}{selected.relatedTantras && selected.relatedTantras.length > 0 && (<div><h4 className="text-xs font-semibold text-accent uppercase tracking-wide mb-1">Related Tantras</h4><div className="flex flex-wrap gap-1.5">{selected.relatedTantras.map((t, i) => <span key={i} className="chip text-[10px]">{t}</span>)}</div></div>)}</div>)}</Modal>
    </div>
  );
}
